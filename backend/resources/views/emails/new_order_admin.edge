<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nouvelle commande</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 20px auto;
      background-color: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .header {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: #ffffff;
      padding: 40px 20px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 28px;
      font-weight: 700;
    }
    .header .emoji {
      font-size: 48px;
      margin-bottom: 10px;
    }
    .alert-box {
      background-color: #d1fae5;
      border-left: 4px solid #10b981;
      padding: 15px 20px;
      margin: 20px 30px;
      border-radius: 4px;
    }
    .content {
      padding: 40px 30px;
    }
    .order-number {
      background-color: #fef3c7;
      border-left: 4px solid #f59e0b;
      padding: 15px 20px;
      margin: 20px 0;
      border-radius: 4px;
      text-align: center;
    }
    .order-number strong {
      color: #d97706;
      font-size: 20px;
    }
    .section {
      margin: 30px 0;
      padding: 20px;
      background-color: #f9fafb;
      border-radius: 8px;
      border: 1px solid #e5e7eb;
    }
    .section-title {
      font-size: 16px;
      font-weight: 600;
      color: #1f2937;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .info-grid {
      display: grid;
      gap: 12px;
    }
    .info-item {
      display: flex;
      gap: 10px;
    }
    .info-label {
      color: #6b7280;
      font-size: 14px;
      min-width: 100px;
      font-weight: 500;
    }
    .info-value {
      color: #111827;
      font-weight: 600;
      flex: 1;
    }
    .product-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }
    .product-table th {
      background-color: #f3f4f6;
      padding: 10px;
      text-align: left;
      font-size: 13px;
      color: #6b7280;
      font-weight: 600;
      border-bottom: 2px solid #e5e7eb;
    }
    .product-table td {
      padding: 12px 10px;
      border-bottom: 1px solid #e5e7eb;
      font-size: 14px;
    }
    .product-name {
      font-weight: 600;
      color: #111827;
    }
    .total-section {
      background: linear-gradient(135deg, #c19a6b 0%, #a67c52 100%);
      color: white;
      padding: 25px;
      border-radius: 8px;
      margin-top: 20px;
      text-align: center;
    }
    .total-label {
      font-size: 14px;
      opacity: 0.9;
      margin-bottom: 8px;
    }
    .total-value {
      font-size: 32px;
      font-weight: 700;
    }
    .action-button {
      display: inline-block;
      padding: 14px 32px;
      background-color: #c19a6b;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      margin-top: 20px;
      text-align: center;
    }
    .action-button:hover {
      background-color: #a67c52;
    }
    .footer {
      background-color: #1f2937;
      color: #9ca3af;
      padding: 30px;
      text-align: center;
      font-size: 13px;
    }
    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      background-color: #fef3c7;
      color: #d97706;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>Nouvelle commande !</h1>
      <p style="margin: 4px 0 0; opacity: 0.9;">Une commande vient d'être passée</p>
    </div>

    <!-- Alert -->
    <div class="alert-box">
      <strong>⚡ Action requise :</strong> Une nouvelle commande nécessite votre attention.
    </div>

    <!-- Content -->
    <div class="content">
      <!-- Order Number -->
      <div class="order-number">
        <div style="font-size: 14px; margin-bottom: 5px;">Numéro de commande</div>
        <strong>{{ order.orderNumber }}</strong>
      </div>

      <div style="text-align: center; margin: 20px 0;">
        <span class="status-badge">{{ order.status === 'pending' ? 'En attente' : order.status }}</span>
      </div>

      <!-- Customer Info -->
      <div class="section">
        <div class="section-title">
          <span>👤</span>
          <span>Informations client</span>
        </div>
        <div class="info-grid">
          <div class="info-item">
            <div class="info-label">Nom :</div>
            <div class="info-value">{{ order.customerName }}</div>
          </div>
          <div class="info-item">
            <div class="info-label">Email :</div>
            <div class="info-value">{{ order.customerEmail }}</div>
          </div>
          <div class="info-item">
            <div class="info-label">Téléphone :</div>
            <div class="info-value">{{ order.customerPhone }}</div>
          </div>
          @if(order.shippingAddress)
          <div class="info-item">
            <div class="info-label">Adresse :</div>
            <div class="info-value" style="white-space: pre-line;">{{ order.shippingAddress }}</div>
          </div>
          @end
          @if(order.notes)
          <div class="info-item">
            <div class="info-label">Notes :</div>
            <div class="info-value">{{ order.notes }}</div>
          </div>
          @end
        </div>
      </div>

      <!-- Products -->
      <div class="section">
        <div class="section-title">
          <span>📦</span>
          <span>Produits commandés</span>
        </div>
        <table class="product-table">
          <thead>
            <tr>
              <th>Produit</th>
              <th style="text-align: center;">Qté</th>
              <th style="text-align: right;">Prix unit.</th>
              <th style="text-align: right;">Total</th>
            </tr>
          </thead>
          <tbody>
            @each(item in order.orderItems)
            <tr>
              <td>
                <div class="product-name">{{ item.product ? item.product.name : 'Produit' }}</div>
              </td>
              <td style="text-align: center;">{{ item.quantity }}</td>
              <td style="text-align: right;">{{ Number(item.unitPrice).toFixed(2) }} €</td>
              <td style="text-align: right; font-weight: 600;">{{ Number(item.totalPrice).toFixed(2) }} €</td>
            </tr>
            @end
          </tbody>
        </table>
      </div>

      <!-- Total -->
      <div class="total-section">
        <div class="total-label">TOTAL DE LA COMMANDE</div>
        <div class="total-value">{{ Number(order.totalAmount).toFixed(2) }} €</div>
      </div>

      <!-- Action Button -->
      <div style="text-align: center; margin-top: 30px;">
        <a href="{{ frontendUrl }}/admin/orders" class="action-button">
          Voir dans l'interface admin
        </a>
      </div>

      <div style="margin-top: 30px; padding: 15px; background-color: #fef3c7; border-radius: 8px; font-size: 13px; color: #92400e;">
        <strong>💡 Conseil :</strong> N'oubliez pas de mettre à jour le statut de la commande une fois le paiement vérifié et les produits préparés.
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p style="margin: 0 0 10px;"><strong>Marché de Noël la Sainte Famille</strong></p>
      <p style="margin: 0;">Interface d'administration</p>
      <p style="margin: 15px 0 0;">&copy; 2024 Tous droits réservés.</p>
    </div>
  </div>
</body>
</html>
